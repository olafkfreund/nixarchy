---
status: approved
issue: 790
spec: spec/2026-09-19-790-herdr-stable.md
---

# Plan: Restore stable evaluation without disabling Herdr

Owner approval: user explicitly authorized continued issue work and all planning/implementation without further approval on 2026-09-19.

## Decisions

Prefer consumer pkgs.herdr; only if absent use the already pinned nixpkgs legacyPackages host-system Herdr. Retain consumer jq/iproute2. No new input, package recipe or host deployment. The fallback is a standalone CLI, a documented narrow exception to same-package-set preference because stable has neither Herdr nor the installAgentSkills hook its current recipe needs.

## Steps

1. Capture the missing attribute through existing stable-eval (historical exact-head CI supplies the initial regression evidence).
2. Add the lazy fallback in modules/home.nix, and an installed-Herdr assertion to tests/stable-eval.nix.
3. Test both paths of the actual selection expression, including fallback laziness and a deliberate removal that fails.
4. Run changed-file format/parse and stable-eval under the shared heavy lock; capture failure and success without running concurrent heavy work.
5. Open a PR with evidence and Closes #790. Preserve unrelated upstream behavior findings.

## Tests

Actual-expression Nix assertions; nixfmt --check; nix-instantiate --parse; git diff --check; nix build .#checks.x86_64-linux.stable-eval --print-build-logs. Stable check proves evaluation and installed package selection, not boot/runtime behavior.

## Rollback

Revert the fix commit; this restores the known stable evaluation failure. No host changes occur.
