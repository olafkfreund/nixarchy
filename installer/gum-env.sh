# gum's colours. Phase 1 uses its defaults deliberately: the palette is chosen
# with the rest of the ISO branding in #18, and picking one here would mean
# picking it twice. Sourced by install.sh so that issue has one place to fill.
#
# shellcheck shell=bash
: "${GUM_CHOOSE_CURSOR_FOREGROUND:=}"
